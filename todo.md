# TODO

- [x] 记录问题 issue：创建房间后，新用户无法读取房间内容；房主添加视频后仅自己可见，其他用户进入房间无法查看。
- [x] 修复 WebSocket 新用户加入房间时缺少房间快照的问题，让新连接能收到当前播放状态、房间用户和视频队列。
- [x] 在房间播放状态中保存视频队列，避免只同步当前操作导致后加入用户缺少队列上下文。
- [x] 在 Go 后端添加管理员调试接口，用于外部环境读取所有房间、房间状态、房间内用户、视频队列和播放进度。
- [x] 为调试接口和新用户加入后的状态同步补充集成测试。
- [ ] 前端需要确认接入 `room_snapshot` 消息，并在加入房间后用该快照初始化页面状态。
- [ ] 若未来需要完整点播队列能力，应补充专门的队列表或房间队列存储，而不是只依赖播放控制消息中的队列字段。
